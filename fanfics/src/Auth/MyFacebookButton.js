import React from "react";

const MyFacebookButton = ({ onClick }) => (
    <button type="button" className="btn btn-outline custom-button sign-in" onClick={onClick}>
        Facebook
    </button>
);
export default MyFacebookButton